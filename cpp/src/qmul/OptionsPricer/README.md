# OptionsPricer

Small C++ project demonstrating a Black-Scholes European call pricer.

Build (out-of-source):

```sh
mkdir -p build && cd build
cmake ../
cmake --build .
./apps/options_pricer
```

Includes:

- Library: `OptionsPricerLib`
- Executable: `options_pricer`
